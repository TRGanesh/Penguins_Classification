with st.sidebar:
   selected = streamlit_option_menu.option_menu(menu_title='Main Menu',options=['Home','Project'],icons=['house','book'],menu_icon='list',default_index=0)

   # LOADING LOTTIE FILE
def load_lottier(url):
    r = requests.get(url)
    if r.status_code != 200:
        return None
    return r.json()
 
in Main
# LOADING LOTTIE FILES
#lottie_coding = load_lottier() 